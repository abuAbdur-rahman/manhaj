# CodeRabbit PR #16 — Trivial Comments

> Auto-generated. 14 trivial items from coderabbitai review `845f26bb`.

## Image `sizes` / `priority`

1. **`app/(admin)/(auth)/admin/login/page.tsx:9-17`** — Logo `Image` uses `fill` without `sizes`. Add `sizes="48px"` and `priority` (above-the-fold LCP candidate).

2. **`components/layout/admin-sidebar.tsx:88-98`** — Sidebar logo `Image` with `fill` lacks `sizes`. Add `sizes="24px"`.

3. **`components/layout/header.tsx:37-46`** — Header logo `Image` with `fill` lacks `sizes` and uses bare `loading="eager"` without preload. Add `sizes="28px"` and `fetchPriority="high"`.

## `viewport` metadata

4. **`app/layout.tsx:60-62`** — Hand-authored `<meta name="theme-color">` in `<head>`. Use Next.js `viewport` export instead.

## Duplicate types

5. **`app/(admin)/(dashboard)/admin/episodes/new/page.tsx:5-16`** — `AdminSeries`/`AdminScholar` duplicated in both `page.tsx` and `new-episode-form.tsx`. Hoist to `types/index.ts`.

6. **`lib/downloads-db.ts:81-106`** — `ManhajDB` value shape duplicates `DownloadedEpisode` from `types/index.ts`. Reuse the shared type.

## Duplicate logic

7. **`app/api/admin/episodes/route.ts:90-171`** — Series-ownership-check logic duplicated between `super_admin` and `scholar_admin` branches. Extract `resolveSeriesOwnership` helper.

## Silent error swallowing

8. **`app/api/search/route.ts:40-61`** — `scholarResult.error` and `seriesResult.error` never checked. Log failures before defaulting to `[]`.

## Destructive button styling

9. **`components/episodes/download-row.tsx:105-123`** — Remove button in confirmation dialog uses `variant="primary"` instead of red warning style (unlike `downloads-content.tsx`).

## AudioCard `useDownloadedIds` per-card

10. **`components/episodes/audio-card.tsx:40-44`** — Each card mounts its own `useDownloadedIds()` → N IndexedDB reads per list page. Lift to page level or use shared cached selector.

## `audioRef` in Zustand

11. **`store/player.ts:12`** — Raw `HTMLAudioElement` stored in reactive state is an anti-pattern. Expose imperative `seek(time)` / `play()` / `pause()` instead.

## `React.cache` for duplicate queries

12. **`app/(public)/lectures/[slug]/page.tsx:13-42`** — `generateMetadata` and `LecturePage` both call `getEpisodeBySlug`. Wrap in `React.cache()` to avoid duplicate Supabase queries.

## IDB version migration

13. **`lib/downloads-db.ts:42-52`** — v1→v2 migration deletes existing store = wipes all saved downloads. Preserve data or handle re-download messaging.

## `blocking()` handler

14. **`lib/downloads-db.ts:33-63`** — `openDB` lacks `blocking()` callback; connection may not close on `versionchange`, blocking future upgrades.
